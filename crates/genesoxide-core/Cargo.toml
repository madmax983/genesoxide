[package]
name = "genesoxide-core"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
proptest = "1"
serde_json = "1"
divan = "0.1"

[[bench]]
name = "frame_throughput"
harness = false
